user patient representative lhcp sort message inbox either sender last timestamp ascending descending order timestamps would recent first outbox recipient selects one option labeled drop box click button note sorted saved later viewing reopened default sorting always used